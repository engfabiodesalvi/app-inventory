# Scripts

## How to grant permission to run a script.

In the terminal, at the root of the project:

```bash
chmod +x scripts/script.sh
```

## How to proceed when necessary.

Still at the root of the project:

```bash
./scripts/script.sh
```
